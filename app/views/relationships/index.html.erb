<% title "Relationships" %>

<table>
  <tr>
    <th>Category</th>
    <th>Synonym</th>
  </tr>
  <% for relationship in @relationships %>
    <tr>
      <td><%=h relationship.category_id %></td>
      <td><%=h relationship.synonym_id %></td>
      <td><%= link_to "Show", relationship %></td>
      <td><%= link_to "Edit", edit_relationship_path(relationship) %></td>
      <td><%= link_to "Destroy", relationship, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Relationship", new_relationship_path %></p>
